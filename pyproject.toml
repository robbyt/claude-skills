[project]
name = "claude-skills"
version = "1.0.0"
description = "Claude Code skills and plugins marketplace"
requires-python = ">=3.11"
dependencies = [
    "claudelint>=0.1.0,<0.2.0",
]

[tool.uv]
dev-dependencies = []
